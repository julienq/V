<svg xmlns="http://www.w3.org/2000/svg"
  xmlns:xlink="http://www.w3.org/1999/xlink"
  viewBox="0 0 400 300">
  <title>Grid</title>
  <star x="200" y="150" r="50" branches="5" phase="90"/>
  <g stroke="black">
    <star x="100" y="70" r="50" branches="6" fill="none" phase="90"/>
    <star x="300" y="220" r="50" branches="8" fill="none"/>
  </g>
  <script xlink:href="../v.js" type="text/javascript"/>
  <script type="text/javascript"><![CDATA[

"use strict";

var svg = document.documentElement;
var bbox = svg.viewBox.baseVal;

var grid = svg.insertBefore(v.$g({ stroke: "black", "stroke-opacity": 0.2 }),
  svg.firstChild);
for (var x = 0; x <= bbox.width; x += 50) {
  grid.appendChild(v.$line({ x1: x, x2: x, y1: 0, y2: bbox.height }));
}
for (var y = 0; y <= bbox.height; y += 50) {
  grid.appendChild(v.$line({ x1: 0, x2: bbox.width, y1: y, y2: y }));
}

  ]]></script>
</svg>

